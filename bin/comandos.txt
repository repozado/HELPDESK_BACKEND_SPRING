# 1. Agregar todos los cambios (archivos nuevos, modificados y eliminados)
git add -A

# 2. Hacer commit con un mensaje
git commit -m "Mensaje descriptivo de los cambios"

# 3. Traer cambios del remoto y rebasar tu rama
git pull --rebase origin master

# 4. Subir tus cambios al remoto
git push origin master


en src/main/resources/
poner en application.properties

spring.application.name=helpdesk_backend_spring

spring.datasource.url=jdbc:postgresql://localhost:5432/nombre_de_la_base_de_datos
spring.datasource.username=postgres
spring.datasource.password=contraseña
spring.datasource.driver-class-name=org.postgresql.Driver

spring.jpa.database=postgresql
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# logging para ver DDL
logging.level.org.hibernate.SQL=DEBUG
logging.level.org.hibernate.type.descriptor.sql.BasicBinder=TRACE

server.port=8090
